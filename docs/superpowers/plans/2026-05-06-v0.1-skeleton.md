# Projector v0.1 Skeleton Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stand up a runnable Spring Boot 4 + jOOQ + Vite/React skeleton for the Projector solo issue tracker — single Docker image, healthcheck reachable, Cucumber smoke pass, CI publishing tagged images to GHCR — without any business logic or schema beyond the audit trigger function.

**Architecture:** One Maven module at the repo root. Spring Boot 4.0.6 on Java 21 serves both the REST API and the built Vite/React SPA from `src/main/resources/static/` inside a single fat jar. Postgres 18 is the only datastore; jOOQ codegen runs against a throwaway Postgres-codegen container during `generate-sources`, generated POJOs go into `target/generated-sources/jooq` (gitignored). Local dev uses `spring-boot-docker-compose` to auto-start `postgres:18-alpine` from `compose.yaml` — no `SPRING_DATASOURCE_*` env vars needed. Tests are split: pure JUnit 5 unit tests (no Spring context, `*Test.java`, surefire) and Cucumber E2E (`*IT.java`, failsafe, hitting a separate Postgres-test container managed by `docker-maven-plugin`). The runtime Dockerfile copies the pre-built jar — it does **not** rebuild the jar.

**Tech Stack:**
- **Backend:** Java 21, Spring Boot 4.0.6 (`spring-boot-starter-webmvc`, `-security`, `-validation`, `-actuator`, `-jooq`, `-flyway`, `-docker-compose`), jOOQ + jOOQ codegen, Flyway + `flyway-database-postgresql`, Micrometer Prometheus registry, springdoc-openapi
- **Frontend:** Vite + React + TypeScript + Tailwind, React Router, TanStack Query, npm
- **DB:** Postgres 18 (image `postgres:18-alpine`)
- **Tests:** JUnit 5 (surefire), Cucumber 7 + cucumber-spring + JUnit Platform Suite (failsafe)
- **Build:** Maven (single module), `frontend-maven-plugin` for Node/SPA, `docker-maven-plugin` (fabric8) for codegen + test Postgres, `flyway-maven-plugin`, `jooq-codegen-maven`, `jacoco-maven-plugin`
- **Runtime image:** `eclipse-temurin:21-jre-alpine` (jar copied in, no build inside Docker)
- **CI:** GitHub Actions — test, build jar, build image; on `main` push to GHCR with two tags (`${project.version}-${shortSha}` and `${project.version}`), never `:latest`

**Coordinates:** `groupId=io.github.pgatzka`, `artifactId=projector`, `version=1.0.0-SNAPSHOT`, `basePackage=io.github.pgatzka.projector`. Frontend `package.json` carries the same version, kept in sync at build time via `npm pkg set version=${project.version}`.

**Out of scope for v0.1:** business tables (account, project, issue, label, comment, activity), authentication, business logic, REST endpoints beyond `/api/health`, real frontend UI beyond a placeholder.

---

## File Structure

| Path | Responsibility |
|---|---|
| `.gitignore` | Excludes `target/` (which contains `generated-sources/jooq/`), `node_modules/`, `frontend/dist/`, IDE files, `HANDOFF.md` |
| `pom.xml` | Single-module Maven build: dependencies, frontend-maven-plugin, docker-maven-plugin, flyway-maven-plugin, jooq-codegen-maven, jacoco, failsafe |
| `compose.yaml` | `postgres:18-alpine` for local dev; consumed by `spring-boot-docker-compose` |
| `Dockerfile` | Runtime-only: `FROM eclipse-temurin:21-jre-alpine`, copy pre-built jar |
| `CLAUDE.md` | Project preferences (jOOQ not JPA, mandatory column block, layer packaging, etc.) — committed to repo |
| `README.md` | How to build, run locally, run tests, deploy |
| `LICENSE` | Already present; preserved |
| `.github/workflows/ci.yml` | Test + jar + image; conditional GHCR push on `main` |
| `docs/jooq-codegen-example/pom.xml` | The user-supplied example codegen pom, moved from repo root |
| `docs/superpowers/plans/2026-05-06-v0.1-skeleton.md` | This plan |
| `src/main/resources/application.yml` | Spring configuration: actuator endpoints, jooq dialect, no datasource (docker-compose feeds it) |
| `src/main/resources/db/migration/V1__create_function_set_updated_at.sql` | The shared updated-at trigger function |
| `src/main/resources/static/.gitkeep` | Placeholder so the directory exists in git; populated at build time by Vite |
| `src/main/java/io/github/pgatzka/projector/ProjectorApplication.java` | `@SpringBootApplication` main class |
| `src/main/java/io/github/pgatzka/projector/config/SecurityConfig.java` | `SecurityFilterChain` permitting actuator, health, static, openapi paths |
| `src/main/java/io/github/pgatzka/projector/config/JooqConfig.java` | Registers `AuditRecordListener` via `DefaultConfigurationCustomizer` |
| `src/main/java/io/github/pgatzka/projector/config/SpaFallbackConfig.java` | Forwards non-`/api/*` non-static routes to `/index.html` so React Router works |
| `src/main/java/io/github/pgatzka/projector/audit/ActorContext.java` | Returns the current `"user:<uuid>"` actor string (v0.1 stub: throws) |
| `src/main/java/io/github/pgatzka/projector/audit/AuditRecordListener.java` | jOOQ `RecordListener` setting `created_by`/`updated_by` automatically |
| `src/main/java/io/github/pgatzka/projector/rest/controller/HealthController.java` | `GET /api/health` returning `{"status":"ok"}` |
| `src/test/java/io/github/pgatzka/projector/unit/HealthControllerTest.java` | Pure JUnit, no Spring — verifies controller returns expected body |
| `src/test/java/io/github/pgatzka/projector/e2e/RunCucumberIT.java` | JUnit Platform Suite that runs all Cucumber features |
| `src/test/java/io/github/pgatzka/projector/e2e/CucumberSpringConfig.java` | `@CucumberContextConfiguration` + `@SpringBootTest` wiring |
| `src/test/java/io/github/pgatzka/projector/e2e/steps/HealthSteps.java` | Step defs for the health feature |
| `src/test/resources/junit-platform.properties` | Cucumber configuration (glue, plugins, quiet publish) |
| `src/test/resources/application-test.yml` | Test profile: datasource pointing at port 25432, docker-compose disabled, Spring Flyway disabled (already migrated) |
| `src/test/resources/features/health.feature` | Gherkin scenario for the health endpoint |
| `frontend/package.json` | npm package, version `1.0.0-SNAPSHOT`, react/router/query/tailwind/vite |
| `frontend/vite.config.ts` | Vite config; outputs to `../src/main/resources/static` |
| `frontend/tsconfig.json` | TS config |
| `frontend/tsconfig.node.json` | TS config for Vite config file |
| `frontend/tailwind.config.ts` | Tailwind content globs + theme |
| `frontend/postcss.config.js` | PostCSS config (Tailwind + autoprefixer) |
| `frontend/index.html` | Vite entry HTML |
| `frontend/src/main.tsx` | React + Router + QueryClient bootstrap |
| `frontend/src/App.tsx` | Placeholder header that fetches `/api/health` |
| `frontend/src/index.css` | Tailwind directives |
| `frontend/src/vite-env.d.ts` | Vite ambient types |
| `frontend/.gitignore` | `node_modules/`, `dist/` |

**Note on layer packaging:** v0.1 only fills `config`, `audit`, `rest.controller`. The empty layer packages (`data.repository`, `data.service`, `rest.service`, `rest.dto`, `rest.exception`) will be created in v0.2 when the first business feature lands. This keeps v0.1 minimal — empty packages without `.gitkeep` files would be silently dropped from git anyway.

---

## Task 1: Bootstrap repo (gitignore, move example pom)

**Files:**
- Create: `.gitignore`
- Move: `example.xml` → `docs/jooq-codegen-example/pom.xml`

- [ ] **Step 1.1: Create `.gitignore`**

```gitignore
# Build output
target/
src/main/resources/static/

# Frontend
node_modules/
frontend/dist/
frontend/.pnpm-debug.log*
frontend/npm-debug.log*

# IDEs
.idea/
*.iml
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Session handoff (per global gitignore convention)
HANDOFF.md
```

- [ ] **Step 1.2: Move `example.xml` to `docs/jooq-codegen-example/pom.xml`**

```bash
mkdir -p docs/jooq-codegen-example
git mv example.xml docs/jooq-codegen-example/pom.xml
```
Expected: `docs/jooq-codegen-example/pom.xml` exists; `example.xml` no longer at repo root.

- [ ] **Step 1.3: Verify**

```bash
ls docs/jooq-codegen-example/
ls -la
```
Expected: `pom.xml` listed under `docs/jooq-codegen-example/`. Repo root contains `.gitignore`, `LICENSE`, `docs/`.

- [ ] **Step 1.4: Commit**

```bash
git add .gitignore docs/jooq-codegen-example/pom.xml
git rm example.xml
git commit -m "chore: gitignore + move example codegen pom under docs"
```

---

## Task 2: Root `pom.xml`

**Files:**
- Create: `pom.xml`

- [ ] **Step 2.1: Create the full `pom.xml`**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.0.6</version>
    <relativePath/>
  </parent>

  <groupId>io.github.pgatzka</groupId>
  <artifactId>projector</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <name>projector</name>
  <description>Solo issue tracker</description>

  <properties>
    <java.version>21</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- Plugin / dependency versions not managed by spring-boot-starter-parent -->
    <docker-maven-plugin.version>0.48.1</docker-maven-plugin.version>
    <frontend-maven-plugin.version>1.15.1</frontend-maven-plugin.version>
    <springdoc.version>3.0.3</springdoc.version>
    <jacoco.version>0.8.13</jacoco.version>
    <node.version>v22.11.0</node.version>

    <!-- Codegen Postgres (used during generate-sources) -->
    <codegen.database>projector_codegen</codegen.database>
    <codegen.port>15432</codegen.port>
    <codegen.user>projector_codegen</codegen.user>
    <codegen.password>projector_codegen</codegen.password>
    <codegen.host>localhost</codegen.host>
    <codegen.url>jdbc:postgresql://${codegen.host}:${codegen.port}/${codegen.database}</codegen.url>

    <!-- Test Postgres (used during integration-test) -->
    <test.database>projector_test</test.database>
    <test.port>25432</test.port>
    <test.user>projector_test</test.user>
    <test.password>projector_test</test.password>
    <test.host>localhost</test.host>
    <test.url>jdbc:postgresql://${test.host}:${test.port}/${test.database}</test.url>
  </properties>

  <dependencies>
    <!-- Web (Spring MVC) -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc</artifactId>
    </dependency>

    <!-- Security -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>

    <!-- Validation -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <!-- Actuator + Prometheus -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-registry-prometheus</artifactId>
    </dependency>

    <!-- jOOQ + datasource -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jooq</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- Flyway -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-flyway</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>

    <!-- spring-boot-docker-compose: auto-starts compose.yaml in dev -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-docker-compose</artifactId>
      <scope>runtime</scope>
      <optional>true</optional>
    </dependency>

    <!-- OpenAPI (springdoc) -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>${springdoc.version}</version>
    </dependency>

    <!-- ============================================================== -->
    <!-- Test                                                              -->
    <!-- ============================================================== -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- Cucumber -->
    <dependency>
      <groupId>io.cucumber</groupId>
      <artifactId>cucumber-java</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.cucumber</groupId>
      <artifactId>cucumber-spring</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.cucumber</groupId>
      <artifactId>cucumber-junit-platform-engine</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.platform</groupId>
      <artifactId>junit-platform-suite</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>io.cucumber</groupId>
        <artifactId>cucumber-bom</artifactId>
        <version>7.20.1</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <plugins>
      <!-- Spring Boot fat jar -->
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>

      <!-- Surefire: unit tests (*Test.java), Spring-context-free -->
      <plugin>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <includes>
            <include>**/*Test.java</include>
          </includes>
        </configuration>
      </plugin>

      <!-- Failsafe: integration tests (*IT.java), Cucumber against test Postgres -->
      <plugin>
        <artifactId>maven-failsafe-plugin</artifactId>
        <configuration>
          <includes>
            <include>**/*IT.java</include>
          </includes>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!-- JaCoCo: coverage report -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals><goal>prepare-agent</goal></goals>
          </execution>
          <execution>
            <id>prepare-agent-integration</id>
            <goals><goal>prepare-agent-integration</goal></goals>
          </execution>
          <execution>
            <id>report</id>
            <phase>verify</phase>
            <goals><goal>report</goal></goals>
          </execution>
        </executions>
      </plugin>

      <!-- ===================================================== -->
      <!-- Frontend build (frontend-maven-plugin)                  -->
      <!-- ===================================================== -->
      <plugin>
        <groupId>com.github.eirslett</groupId>
        <artifactId>frontend-maven-plugin</artifactId>
        <version>${frontend-maven-plugin.version}</version>
        <configuration>
          <workingDirectory>frontend</workingDirectory>
          <installDirectory>target</installDirectory>
        </configuration>
        <executions>
          <execution>
            <id>install-node-and-npm</id>
            <goals><goal>install-node-and-npm</goal></goals>
            <phase>validate</phase>
            <configuration>
              <nodeVersion>${node.version}</nodeVersion>
            </configuration>
          </execution>
          <execution>
            <id>sync-frontend-version</id>
            <goals><goal>npm</goal></goals>
            <phase>validate</phase>
            <configuration>
              <arguments>pkg set version=${project.version}</arguments>
            </configuration>
          </execution>
          <execution>
            <id>frontend-install</id>
            <goals><goal>npm</goal></goals>
            <phase>generate-resources</phase>
            <configuration>
              <arguments>install</arguments>
            </configuration>
          </execution>
          <execution>
            <id>frontend-build</id>
            <goals><goal>npm</goal></goals>
            <phase>generate-resources</phase>
            <configuration>
              <arguments>run build</arguments>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- ===================================================== -->
      <!-- Codegen + test Postgres (docker-maven-plugin)           -->
      <!-- ===================================================== -->
      <plugin>
        <groupId>io.fabric8</groupId>
        <artifactId>docker-maven-plugin</artifactId>
        <version>${docker-maven-plugin.version}</version>
        <configuration>
          <volumes>
            <volume><name>projector-codegen_postgres</name></volume>
            <volume><name>projector-test_postgres</name></volume>
          </volumes>
          <removeVolumes>true</removeVolumes>
          <images>
            <image>
              <alias>postgres_codegen</alias>
              <name>postgres:18-alpine</name>
              <run>
                <containerNamePattern>projector-codegen_postgres</containerNamePattern>
                <env>
                  <POSTGRES_DB>${codegen.database}</POSTGRES_DB>
                  <POSTGRES_USER>${codegen.user}</POSTGRES_USER>
                  <POSTGRES_PASSWORD>${codegen.password}</POSTGRES_PASSWORD>
                </env>
                <ports>
                  <port>${codegen.port}:5432</port>
                </ports>
                <volumes>
                  <bind>
                    <volume>projector-codegen_postgres:/var/lib/postgresql</volume>
                  </bind>
                </volumes>
                <wait>
                  <log>listening on IPv4 address</log>
                  <time>30000</time>
                </wait>
              </run>
            </image>
            <image>
              <alias>postgres_test</alias>
              <name>postgres:18-alpine</name>
              <run>
                <containerNamePattern>projector-test_postgres</containerNamePattern>
                <env>
                  <POSTGRES_DB>${test.database}</POSTGRES_DB>
                  <POSTGRES_USER>${test.user}</POSTGRES_USER>
                  <POSTGRES_PASSWORD>${test.password}</POSTGRES_PASSWORD>
                </env>
                <ports>
                  <port>${test.port}:5432</port>
                </ports>
                <volumes>
                  <bind>
                    <volume>projector-test_postgres:/var/lib/postgresql</volume>
                  </bind>
                </volumes>
                <wait>
                  <log>listening on IPv4 address</log>
                  <time>30000</time>
                </wait>
              </run>
            </image>
          </images>
        </configuration>
        <executions>
          <execution>
            <id>start-codegen-database</id>
            <phase>initialize</phase>
            <goals><goal>stop</goal><goal>start</goal></goals>
            <configuration><filter>postgres_codegen</filter></configuration>
          </execution>
          <execution>
            <id>stop-codegen-database</id>
            <phase>process-sources</phase>
            <goals><goal>stop</goal></goals>
            <configuration><filter>postgres_codegen</filter></configuration>
          </execution>
          <execution>
            <id>start-test-database</id>
            <phase>pre-integration-test</phase>
            <goals><goal>stop</goal><goal>start</goal></goals>
            <configuration><filter>postgres_test</filter></configuration>
          </execution>
          <execution>
            <id>stop-test-database</id>
            <phase>post-integration-test</phase>
            <goals><goal>stop</goal></goals>
            <configuration><filter>postgres_test</filter></configuration>
          </execution>
          <execution>
            <id>remove-containers</id>
            <phase>clean</phase>
            <goals><goal>remove</goal></goals>
          </execution>
          <execution>
            <id>remove-volumes</id>
            <phase>clean</phase>
            <goals><goal>volume-remove</goal></goals>
          </execution>
        </executions>
      </plugin>

      <!-- ===================================================== -->
      <!-- Flyway: migrate codegen + test databases                -->
      <!-- ===================================================== -->
      <plugin>
        <groupId>org.flywaydb</groupId>
        <artifactId>flyway-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>migrate-codegen-database</id>
            <phase>generate-sources</phase>
            <goals><goal>migrate</goal></goals>
            <configuration>
              <url>${codegen.url}</url>
              <user>${codegen.user}</user>
              <password>${codegen.password}</password>
            </configuration>
          </execution>
          <execution>
            <id>migrate-test-database</id>
            <phase>pre-integration-test</phase>
            <goals><goal>migrate</goal></goals>
            <configuration>
              <url>${test.url}</url>
              <user>${test.user}</user>
              <password>${test.password}</password>
            </configuration>
          </execution>
        </executions>
        <dependencies>
          <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
            <version>${flyway.version}</version>
          </dependency>
        </dependencies>
      </plugin>

      <!-- ===================================================== -->
      <!-- jOOQ codegen                                            -->
      <!-- ===================================================== -->
      <plugin>
        <groupId>org.jooq</groupId>
        <artifactId>jooq-codegen-maven</artifactId>
        <configuration>
          <jdbc>
            <url>${codegen.url}</url>
            <user>${codegen.user}</user>
            <password>${codegen.password}</password>
          </jdbc>
          <generator>
            <database>
              <name>org.jooq.meta.postgres.PostgresDatabase</name>
              <inputSchema>public</inputSchema>
              <excludes>flyway_schema_history</excludes>
              <catalogVersionProvider>select max(version) from flyway_schema_history</catalogVersionProvider>
              <schemaVersionProvider>select max(version) from flyway_schema_history</schemaVersionProvider>
            </database>
            <generate>
              <pojos>true</pojos>
              <fluentSetters>true</fluentSetters>
            </generate>
            <target>
              <packageName>io.github.pgatzka.projector.jooq</packageName>
              <directory>${project.build.directory}/generated-sources/jooq</directory>
            </target>
          </generator>
        </configuration>
        <executions>
          <execution>
            <id>codegen-generate-code</id>
            <phase>generate-sources</phase>
            <goals><goal>generate</goal></goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
```

- [ ] **Step 2.2: Verify pom is well-formed and dependencies resolve**

Run: `mvn validate -DskipTests`
Expected: `BUILD SUCCESS`. No "could not resolve" errors. (Network access required.)

- [ ] **Step 2.3: Commit**

```bash
git add pom.xml
git commit -m "build: pom.xml with spring-boot 4.0.6, jooq codegen, frontend, test Postgres"
```

---

## Task 3: Application config + main class + V1 migration

**Files:**
- Create: `src/main/resources/application.yml`
- Create: `src/main/resources/db/migration/V1__create_function_set_updated_at.sql`
- Create: `src/main/resources/static/.gitkeep`
- Create: `src/main/java/io/github/pgatzka/projector/ProjectorApplication.java`

- [ ] **Step 3.1: Create `src/main/resources/application.yml`**

```yaml
server:
  port: 8080

spring:
  application:
    name: projector
  jooq:
    sql-dialect: postgres
  mvc:
    static-path-pattern: /**

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
  prometheus:
    metrics:
      export:
        enabled: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

logging:
  level:
    root: INFO
    io.github.pgatzka.projector: DEBUG
```

Note: no `spring.datasource.*` — `spring-boot-docker-compose` reads connection details from the running `postgres:18-alpine` container in dev. In prod, the operator wires the datasource via env vars or their own deployment compose file.

- [ ] **Step 3.2: Create the V1 Flyway migration**

File: `src/main/resources/db/migration/V1__create_function_set_updated_at.sql`

```sql
create or replace function set_updated_at()
returns trigger
language plpgsql as $$
begin
  if (to_jsonb(new) - 'updated_at' - 'updated_by' - 'version')
     is distinct from
     (to_jsonb(old) - 'updated_at' - 'updated_by' - 'version')
  then
    new.updated_at := current_timestamp;
  end if;
  return new;
end
$$;
```

- [ ] **Step 3.3: Create `src/main/resources/static/.gitkeep`**

Empty file. Ensures the directory exists in git so Spring's static resource handler has a target before the frontend build runs.

```bash
touch src/main/resources/static/.gitkeep
```

- [ ] **Step 3.4: Create `ProjectorApplication.java`**

File: `src/main/java/io/github/pgatzka/projector/ProjectorApplication.java`

```java
package io.github.pgatzka.projector;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class ProjectorApplication {

    public static void main(String[] args) {
        SpringApplication.run(ProjectorApplication.class, args);
    }
}
```

- [ ] **Step 3.5: Verify it compiles**

Run: `mvn -o compile -DskipTests` (offline if cached; drop `-o` if first compile)
Expected: `BUILD SUCCESS`. (jOOQ codegen will run too — it'll start the codegen Postgres, migrate, generate an empty schema package, and stop the container. This may take 30–60s on first run.)

- [ ] **Step 3.6: Commit**

```bash
git add src/main/resources/application.yml \
        src/main/resources/db/migration/V1__create_function_set_updated_at.sql \
        src/main/resources/static/.gitkeep \
        src/main/java/io/github/pgatzka/projector/ProjectorApplication.java
git commit -m "feat: application.yml, V1 set_updated_at trigger function, main class"
```

---

## Task 4: SecurityConfig

**Files:**
- Create: `src/main/java/io/github/pgatzka/projector/config/SecurityConfig.java`

- [ ] **Step 4.1: Create `SecurityConfig.java`**

```java
package io.github.pgatzka.projector.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.config.annotation.web.configuration.EnableWebSecurity;
import org.springframework.security.web.SecurityFilterChain;

@Configuration
@EnableWebSecurity
public class SecurityConfig {

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .authorizeHttpRequests(authz -> authz
                .requestMatchers(
                    "/", "/index.html", "/favicon.ico",
                    "/assets/**", "/static/**",
                    "/api/health",
                    "/actuator/health", "/actuator/health/**",
                    "/actuator/info",
                    "/actuator/prometheus",
                    "/v3/api-docs/**",
                    "/swagger-ui/**", "/swagger-ui.html"
                ).permitAll()
                .anyRequest().authenticated()
            )
            .csrf(csrf -> csrf.disable())
            .formLogin(form -> form.disable())
            .httpBasic(basic -> basic.disable());
        return http.build();
    }
}
```

- [ ] **Step 4.2: Verify it compiles**

Run: `mvn compile -DskipTests`
Expected: `BUILD SUCCESS`.

- [ ] **Step 4.3: Commit**

```bash
git add src/main/java/io/github/pgatzka/projector/config/SecurityConfig.java
git commit -m "feat(security): SecurityFilterChain permitting actuator + static + openapi"
```

---

## Task 5: Audit infrastructure (ActorContext + RecordListener + JooqConfig)

**Files:**
- Create: `src/main/java/io/github/pgatzka/projector/audit/ActorContext.java`
- Create: `src/main/java/io/github/pgatzka/projector/audit/AuditRecordListener.java`
- Create: `src/main/java/io/github/pgatzka/projector/config/JooqConfig.java`

- [ ] **Step 5.1: Create `ActorContext.java`**

```java
package io.github.pgatzka.projector.audit;

import org.springframework.stereotype.Component;

/**
 * Returns the current actor as the canonical "user:&lt;uuid&gt;" string used in
 * the created_by / updated_by audit columns.
 *
 * v0.1: no inserts/updates happen yet, so calling currentActor() throws.
 * v0.2 will replace this with a SecurityContextHolder-backed implementation.
 */
@Component
public class ActorContext {

    public String currentActor() {
        throw new IllegalStateException(
            "ActorContext.currentActor() called in v0.1 — no authenticated principal yet. "
            + "This will be implemented in v0.2 once auth lands."
        );
    }
}
```

- [ ] **Step 5.2: Create `AuditRecordListener.java`**

```java
package io.github.pgatzka.projector.audit;

import org.jooq.Field;
import org.jooq.Record;
import org.jooq.RecordContext;
import org.jooq.RecordListener;
import org.springframework.stereotype.Component;

/**
 * Sets created_by on insert and updated_by on update from the current
 * ActorContext. Audit columns the table doesn't have are silently skipped.
 */
@Component
public class AuditRecordListener implements RecordListener {

    private final ActorContext actorContext;

    public AuditRecordListener(ActorContext actorContext) {
        this.actorContext = actorContext;
    }

    @Override
    public void insertStart(RecordContext ctx) {
        String actor = actorContext.currentActor();
        setIfPresent(ctx.record(), "created_by", actor);
        setIfPresent(ctx.record(), "updated_by", actor);
    }

    @Override
    public void updateStart(RecordContext ctx) {
        setIfPresent(ctx.record(), "updated_by", actorContext.currentActor());
    }

    @SuppressWarnings({"unchecked", "rawtypes"})
    private static void setIfPresent(Record record, String name, String value) {
        Field<?> field = record.field(name);
        if (field != null) {
            record.set((Field) field, value);
        }
    }
}
```

- [ ] **Step 5.3: Create `JooqConfig.java`**

```java
package io.github.pgatzka.projector.config;

import io.github.pgatzka.projector.audit.AuditRecordListener;
import org.jooq.impl.DefaultRecordListenerProvider;
import org.springframework.boot.jooq.autoconfigure.DefaultConfigurationCustomizer;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class JooqConfig {

    @Bean
    public DefaultConfigurationCustomizer jooqConfigurationCustomizer(AuditRecordListener listener) {
        return config -> config.set(new DefaultRecordListenerProvider(listener));
    }
}
```

- [ ] **Step 5.4: Verify it compiles**

Run: `mvn compile -DskipTests`
Expected: `BUILD SUCCESS`.

- [ ] **Step 5.5: Commit**

```bash
git add src/main/java/io/github/pgatzka/projector/audit/ \
        src/main/java/io/github/pgatzka/projector/config/JooqConfig.java
git commit -m "feat(audit): ActorContext stub + AuditRecordListener + JooqConfig wiring"
```

---

## Task 6: HealthController (TDD)

**Files:**
- Create: `src/test/java/io/github/pgatzka/projector/unit/HealthControllerTest.java`
- Create: `src/main/java/io/github/pgatzka/projector/rest/controller/HealthController.java`

- [ ] **Step 6.1: Write the failing unit test (no Spring context)**

File: `src/test/java/io/github/pgatzka/projector/unit/HealthControllerTest.java`

```java
package io.github.pgatzka.projector.unit;

import io.github.pgatzka.projector.rest.controller.HealthController;
import org.junit.jupiter.api.Test;

import java.util.Map;

import static org.assertj.core.api.Assertions.assertThat;

class HealthControllerTest {

    @Test
    void healthReturnsStatusOk() {
        HealthController controller = new HealthController();

        Map<String, String> body = controller.health();

        assertThat(body).containsEntry("status", "ok");
    }
}
```

- [ ] **Step 6.2: Run the test to verify it fails**

Run: `mvn -Dtest=HealthControllerTest test`
Expected: compilation failure — `HealthController` cannot be resolved.

- [ ] **Step 6.3: Implement `HealthController.java`**

File: `src/main/java/io/github/pgatzka/projector/rest/controller/HealthController.java`

```java
package io.github.pgatzka.projector.rest.controller;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.Map;

@RestController
public class HealthController {

    @GetMapping("/api/health")
    public Map<String, String> health() {
        return Map.of("status", "ok");
    }
}
```

- [ ] **Step 6.4: Run the test to verify it passes**

Run: `mvn -Dtest=HealthControllerTest test`
Expected: `Tests run: 1, Failures: 0, Errors: 0, Skipped: 0`. `BUILD SUCCESS`.

- [ ] **Step 6.5: Commit**

```bash
git add src/test/java/io/github/pgatzka/projector/unit/HealthControllerTest.java \
        src/main/java/io/github/pgatzka/projector/rest/controller/HealthController.java
git commit -m "feat(rest): /api/health endpoint with unit test"
```

---

## Task 7: SPA fallback for React Router

**Files:**
- Create: `src/main/java/io/github/pgatzka/projector/config/SpaFallbackConfig.java`

- [ ] **Step 7.1: Create `SpaFallbackConfig.java`**

```java
package io.github.pgatzka.projector.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.ViewControllerRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

/**
 * Forwards unmatched non-API routes to /index.html so the React Router SPA
 * can handle client-side routing. /api/** and /actuator/** keep their normal
 * 404 behavior because they're handled by controllers / actuator before this
 * fallback fires.
 */
@Configuration
public class SpaFallbackConfig implements WebMvcConfigurer {

    @Override
    public void addViewControllers(ViewControllerRegistry registry) {
        // Single-segment paths (e.g. /projects, /settings)
        registry.addViewController("/{path:^(?!api|actuator|assets|static|v3|swagger-ui|favicon\\.ico|index\\.html).*$}")
                .setViewName("forward:/index.html");
        // Two-or-more-segment paths (e.g. /projects/abc, /issues/abc/edit)
        registry.addViewController("/{path:^(?!api|actuator|assets|static|v3|swagger-ui).*$}/**")
                .setViewName("forward:/index.html");
    }
}
```

- [ ] **Step 7.2: Verify it compiles**

Run: `mvn compile -DskipTests`
Expected: `BUILD SUCCESS`.

- [ ] **Step 7.3: Commit**

```bash
git add src/main/java/io/github/pgatzka/projector/config/SpaFallbackConfig.java
git commit -m "feat(web): SPA fallback so React Router routes resolve to index.html"
```

---

## Task 8: Cucumber E2E infrastructure

**Files:**
- Create: `src/test/java/io/github/pgatzka/projector/e2e/RunCucumberIT.java`
- Create: `src/test/java/io/github/pgatzka/projector/e2e/CucumberSpringConfig.java`
- Create: `src/test/resources/junit-platform.properties`
- Create: `src/test/resources/application-test.yml`

- [ ] **Step 8.1: Create `RunCucumberIT.java`**

```java
package io.github.pgatzka.projector.e2e;

import org.junit.platform.suite.api.ConfigurationParameter;
import org.junit.platform.suite.api.IncludeEngines;
import org.junit.platform.suite.api.SelectClasspathResource;
import org.junit.platform.suite.api.Suite;

import static io.cucumber.core.options.Constants.GLUE_PROPERTY_NAME;
import static io.cucumber.core.options.Constants.PLUGIN_PROPERTY_NAME;

@Suite
@IncludeEngines("cucumber")
@SelectClasspathResource("features")
@ConfigurationParameter(key = PLUGIN_PROPERTY_NAME, value = "pretty, summary")
@ConfigurationParameter(key = GLUE_PROPERTY_NAME, value = "io.github.pgatzka.projector.e2e")
public class RunCucumberIT {
}
```

- [ ] **Step 8.2: Create `CucumberSpringConfig.java`**

```java
package io.github.pgatzka.projector.e2e;

import io.cucumber.spring.CucumberContextConfiguration;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;

@CucumberContextConfiguration
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
@ActiveProfiles("test")
public class CucumberSpringConfig {
}
```

- [ ] **Step 8.3: Create `src/test/resources/junit-platform.properties`**

```properties
cucumber.publish.quiet=true
cucumber.execution.parallel.enabled=false
```

- [ ] **Step 8.4: Create `src/test/resources/application-test.yml`**

```yaml
spring:
  docker:
    compose:
      enabled: false
  datasource:
    url: jdbc:postgresql://localhost:25432/projector_test
    username: projector_test
    password: projector_test
  flyway:
    enabled: false
```

Reasoning:
- `spring.docker.compose.enabled: false` — the test postgres is managed by `docker-maven-plugin`, not by `spring-boot-docker-compose`. We don't want Spring trying to start `compose.yaml`'s dev postgres on port 5432 during tests.
- Hardcoded datasource pointing at port `25432` (the test container).
- `spring.flyway.enabled: false` — `flyway-maven-plugin` already migrated the test DB in the `pre-integration-test` phase. Spring's auto-Flyway would re-run on each `@SpringBootTest` startup; harmless (idempotent) but wasteful.

- [ ] **Step 8.5: Verify it compiles**

Run: `mvn test-compile`
Expected: `BUILD SUCCESS`.

- [ ] **Step 8.6: Commit**

```bash
git add src/test/java/io/github/pgatzka/projector/e2e/ \
        src/test/resources/junit-platform.properties \
        src/test/resources/application-test.yml
git commit -m "test: Cucumber JUnit Platform Suite + Spring Boot test wiring"
```

---

## Task 9: First Cucumber scenario (TDD: health.feature)

**Files:**
- Create: `src/test/resources/features/health.feature`
- Create: `src/test/java/io/github/pgatzka/projector/e2e/steps/HealthSteps.java`

- [ ] **Step 9.1: Write the failing feature**

File: `src/test/resources/features/health.feature`

```gherkin
Feature: Health endpoint

  Scenario: Health endpoint returns ok
    When I request GET "/api/health"
    Then the response status is 200
    And the response body field "status" equals "ok"
```

- [ ] **Step 9.2: Run failsafe to verify the scenario fails (no step defs yet)**

Run: `mvn verify -DskipUnitTests=false`
Expected: `BUILD FAILURE`. Cucumber reports "Undefined steps". Suggested step snippets are printed.

- [ ] **Step 9.3: Implement `HealthSteps.java`**

File: `src/test/java/io/github/pgatzka/projector/e2e/steps/HealthSteps.java`

```java
package io.github.pgatzka.projector.e2e.steps;

import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.web.client.TestRestTemplate;
import org.springframework.boot.test.web.server.LocalServerPort;
import org.springframework.core.ParameterizedTypeReference;
import org.springframework.http.HttpMethod;
import org.springframework.http.ResponseEntity;

import java.util.Map;

import static org.assertj.core.api.Assertions.assertThat;

public class HealthSteps {

    @LocalServerPort
    private int port;

    @Autowired
    private TestRestTemplate restTemplate;

    private ResponseEntity<Map<String, Object>> response;

    @When("I request GET {string}")
    public void iRequestGet(String path) {
        response = restTemplate.exchange(
            "http://localhost:" + port + path,
            HttpMethod.GET,
            null,
            new ParameterizedTypeReference<Map<String, Object>>() {}
        );
    }

    @Then("the response status is {int}")
    public void theResponseStatusIs(int status) {
        assertThat(response.getStatusCode().value()).isEqualTo(status);
    }

    @Then("the response body field {string} equals {string}")
    public void theResponseBodyFieldEquals(String field, String value) {
        assertThat(response.getBody()).containsEntry(field, value);
    }
}
```

- [ ] **Step 9.4: Run failsafe to verify the scenario passes**

Run: `mvn verify`
Expected:
```
1 Scenarios (1 passed)
3 Steps (3 passed)
...
[INFO] BUILD SUCCESS
```

The whole pipeline runs:
1. Codegen Postgres starts → Flyway migrates → jOOQ codegen → codegen Postgres stops
2. Compile + surefire (HealthControllerTest passes)
3. Test Postgres starts → Flyway migrates → failsafe runs Cucumber against `RANDOM_PORT` Spring Boot app → test Postgres stops
4. Jar packaged at `target/projector-1.0.0-SNAPSHOT.jar`

(First run may take 2–5 minutes downloading Node, dependencies, Postgres image.)

- [ ] **Step 9.5: Commit**

```bash
git add src/test/resources/features/health.feature \
        src/test/java/io/github/pgatzka/projector/e2e/steps/HealthSteps.java
git commit -m "test(e2e): cucumber scenario covering /api/health"
```

---

## Task 10: Frontend scaffold (configs)

**Files:**
- Create: `frontend/.gitignore`
- Create: `frontend/package.json`
- Create: `frontend/tsconfig.json`
- Create: `frontend/tsconfig.node.json`
- Create: `frontend/vite.config.ts`
- Create: `frontend/postcss.config.js`
- Create: `frontend/tailwind.config.ts`

- [ ] **Step 10.1: Create `frontend/.gitignore`**

```gitignore
node_modules/
dist/
.pnpm-debug.log*
npm-debug.log*
*.tsbuildinfo
```

- [ ] **Step 10.2: Create `frontend/package.json`**

```json
{
  "name": "projector-frontend",
  "private": true,
  "version": "1.0.0-SNAPSHOT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.62.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.15",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
```

- [ ] **Step 10.3: Create `frontend/tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "Bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": "./src",
    "paths": { "@/*": ["*"] }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

- [ ] **Step 10.4: Create `frontend/tsconfig.node.json`**

```json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true
  },
  "include": ["vite.config.ts"]
}
```

- [ ] **Step 10.5: Create `frontend/vite.config.ts`**

```ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import path from "node:path";
import { fileURLToPath } from "node:url";

// ESM doesn't have __dirname; derive from import.meta.url
const __dirname = path.dirname(fileURLToPath(import.meta.url));

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: { "@": path.resolve(__dirname, "src") },
  },
  build: {
    // Output into Spring Boot's static resources so the fat jar contains the SPA.
    outDir: path.resolve(__dirname, "../src/main/resources/static"),
    emptyOutDir: true,
  },
  server: {
    port: 5173,
    proxy: {
      "/api": "http://localhost:8080",
      "/actuator": "http://localhost:8080",
    },
  },
});
```

- [ ] **Step 10.6: Create `frontend/postcss.config.js`**

```js
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

- [ ] **Step 10.7: Create `frontend/tailwind.config.ts`**

```ts
import type { Config } from "tailwindcss";

export default {
  content: ["./index.html", "./src/**/*.{ts,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
} satisfies Config;
```

- [ ] **Step 10.8: Commit**

```bash
git add frontend/.gitignore frontend/package.json \
        frontend/tsconfig.json frontend/tsconfig.node.json \
        frontend/vite.config.ts frontend/postcss.config.js frontend/tailwind.config.ts
git commit -m "chore(frontend): scaffold (vite, react, ts, tailwind, tanstack-query, router)"
```

---

## Task 11: Frontend stub UI

**Files:**
- Create: `frontend/index.html`
- Create: `frontend/src/main.tsx`
- Create: `frontend/src/App.tsx`
- Create: `frontend/src/index.css`
- Create: `frontend/src/vite-env.d.ts`

- [ ] **Step 11.1: Create `frontend/index.html`**

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Projector</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 11.2: Create `frontend/src/index.css`**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

html, body, #root {
  height: 100%;
}
```

- [ ] **Step 11.3: Create `frontend/src/vite-env.d.ts`**

```ts
/// <reference types="vite/client" />
```

- [ ] **Step 11.4: Create `frontend/src/main.tsx`**

```tsx
import React from "react";
import ReactDOM from "react-dom/client";
import { BrowserRouter } from "react-router-dom";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import App from "./App";
import "./index.css";

const queryClient = new QueryClient();

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <QueryClientProvider client={queryClient}>
      <BrowserRouter>
        <App />
      </BrowserRouter>
    </QueryClientProvider>
  </React.StrictMode>,
);
```

- [ ] **Step 11.5: Create `frontend/src/App.tsx`**

```tsx
import { useQuery } from "@tanstack/react-query";

interface Health {
  status: string;
}

async function fetchHealth(): Promise<Health> {
  const res = await fetch("/api/health");
  if (!res.ok) throw new Error(`health check failed: ${res.status}`);
  return res.json();
}

export default function App() {
  const { data, isLoading, isError, error } = useQuery({
    queryKey: ["health"],
    queryFn: fetchHealth,
  });

  return (
    <div className="min-h-full bg-slate-50 text-slate-900">
      <header className="border-b border-slate-200 bg-white px-6 py-4">
        <h1 className="text-xl font-semibold">Projector</h1>
        <p className="text-sm text-slate-500">v0.1 skeleton</p>
      </header>
      <main className="px-6 py-8">
        <section className="max-w-md rounded border border-slate-200 bg-white p-4">
          <h2 className="mb-2 text-sm font-medium text-slate-500">Backend health</h2>
          {isLoading && <p>Checking…</p>}
          {isError && <p className="text-red-600">Error: {(error as Error).message}</p>}
          {data && (
            <p className="font-mono text-lg">
              status: <span className="text-emerald-600">{data.status}</span>
            </p>
          )}
        </section>
      </main>
    </div>
  );
}
```

- [ ] **Step 11.6: Verify the frontend builds standalone**

Run:
```bash
cd frontend
npm install
npm run build
cd ..
```
Expected: `npm install` completes, `npm run build` writes assets into `src/main/resources/static/`. `ls src/main/resources/static/` shows `index.html`, `assets/` directory.

- [ ] **Step 11.7: Verify the full Maven build packages the SPA into the jar**

Run: `mvn -DskipITs package`
Expected: `BUILD SUCCESS`. `target/projector-1.0.0-SNAPSHOT.jar` exists. Verify SPA is inside:
```bash
unzip -l target/projector-1.0.0-SNAPSHOT.jar | grep -E "static/(index\.html|assets/)"
```
Expected: lines for `BOOT-INF/classes/static/index.html` and at least one file under `BOOT-INF/classes/static/assets/`.

- [ ] **Step 11.8: Commit**

```bash
git add frontend/index.html frontend/src/
git commit -m "feat(frontend): stub UI fetching /api/health via TanStack Query"
```

---

## Task 12: Dockerfile (runtime only)

**Files:**
- Create: `Dockerfile`
- Create: `.dockerignore`

- [ ] **Step 12.1: Create `Dockerfile`**

```dockerfile
# Runtime-only image. The jar must be built first with `mvn package`.
# This Dockerfile does NOT compile Java or build the frontend.
FROM eclipse-temurin:21-jre-alpine

WORKDIR /app

# Non-root user for safety
RUN addgroup -S projector && adduser -S projector -G projector
USER projector

# Copy the pre-built fat jar (built by Maven, already contains the SPA)
COPY --chown=projector:projector target/projector-*.jar /app/app.jar

EXPOSE 8080

ENTRYPOINT ["java", "-jar", "/app/app.jar"]
```

- [ ] **Step 12.2: Create `.dockerignore`**

```
# Everything by default
*

# Allow only the jar
!target/projector-*.jar
```

This minimises the build context — Docker only sees the jar, not the entire repo.

- [ ] **Step 12.3: Verify the image builds**

Run: `docker build -t projector:dev .`
Expected: `Successfully tagged projector:dev`. Image size ~200–250 MB (JRE + jar).

- [ ] **Step 12.4: Commit**

```bash
git add Dockerfile .dockerignore
git commit -m "build: runtime-only Dockerfile (uses pre-built jar)"
```

---

## Task 13: `compose.yaml` for local dev

**Files:**
- Create: `compose.yaml`

- [ ] **Step 13.1: Create `compose.yaml`**

```yaml
# Consumed by spring-boot-docker-compose during `mvn spring-boot:run`.
# Postgres connection details are read from the running container — Spring
# auto-configures spring.datasource.* from POSTGRES_DB/USER/PASSWORD.

services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: projector
      POSTGRES_USER: projector
      POSTGRES_PASSWORD: projector
    ports:
      - "5432:5432"
    volumes:
      - projector_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U projector -d projector"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  projector_postgres: {}
```

- [ ] **Step 13.2: Verify Spring boots and connects (manual sanity check)**

Run: `mvn spring-boot:run`

Expected log lines (in order):
1. `spring-boot-docker-compose` detects `compose.yaml`, starts the postgres container
2. Flyway runs `V1__create_function_set_updated_at.sql` against the dev DB
3. Spring Boot starts on port 8080
4. `Started ProjectorApplication`

Then in another terminal:
```bash
curl -s http://localhost:8080/api/health
```
Expected: `{"status":"ok"}`

```bash
curl -s http://localhost:8080/actuator/health
```
Expected: `{"status":"UP",...}`

```bash
curl -s http://localhost:8080/actuator/prometheus | head -5
```
Expected: prometheus metric lines starting with `# HELP ...`.

Stop with `Ctrl+C`. spring-boot-docker-compose stops the container automatically.

- [ ] **Step 13.3: Commit**

```bash
git add compose.yaml
git commit -m "build: compose.yaml with postgres:18-alpine for spring-boot-docker-compose"
```

---

## Task 14: GitHub Actions CI

**Files:**
- Create: `.github/workflows/ci.yml`

- [ ] **Step 14.1: Create `.github/workflows/ci.yml`**

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Build, test, package (jar)
        run: mvn --batch-mode --no-transfer-progress verify

      - name: Read project version
        id: version
        run: |
          VERSION=$(mvn -q -Dexec.executable=echo -Dexec.args='${project.version}' --non-recursive exec:exec)
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          SHORT_SHA="${GITHUB_SHA:0:7}"
          echo "short_sha=${SHORT_SHA}" >> "$GITHUB_OUTPUT"

      - name: Build Docker image
        run: |
          docker build -t projector:${{ steps.version.outputs.version }}-${{ steps.version.outputs.short_sha }} .
          docker tag projector:${{ steps.version.outputs.version }}-${{ steps.version.outputs.short_sha }} \
                     projector:${{ steps.version.outputs.version }}

      - name: Log in to GHCR
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Tag and push to GHCR
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          REPO_LC=$(echo "${{ env.IMAGE_NAME }}" | tr '[:upper:]' '[:lower:]')
          VERSION="${{ steps.version.outputs.version }}"
          SHORT_SHA="${{ steps.version.outputs.short_sha }}"

          docker tag projector:${VERSION}-${SHORT_SHA} ${{ env.REGISTRY }}/${REPO_LC}:${VERSION}-${SHORT_SHA}
          docker tag projector:${VERSION}             ${{ env.REGISTRY }}/${REPO_LC}:${VERSION}

          docker push ${{ env.REGISTRY }}/${REPO_LC}:${VERSION}-${SHORT_SHA}
          docker push ${{ env.REGISTRY }}/${REPO_LC}:${VERSION}
```

Reasoning:
- `verify` runs unit + integration tests + builds jar.
- Image is built on every push/PR (catches Dockerfile bugs early), but only pushed on `main`.
- Two tags pushed: immutable per-commit (`${version}-${sha}`) and moving per-version (`${version}`).
- Never `:latest`.
- `docker-maven-plugin` (fabric8) connects to the GitHub runner's Docker daemon for the codegen + test postgres containers; this works out of the box on `ubuntu-latest`.

- [ ] **Step 14.2: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: GitHub Actions test+jar+image, GHCR push on main with two tags"
```

---

## Task 15: `CLAUDE.md` capturing project preferences

**Files:**
- Create: `CLAUDE.md`

- [ ] **Step 15.1: Create `CLAUDE.md`**

```markdown
# Projector — Project Preferences

This file is the source of truth for durable conventions in this repo. Read before suggesting changes that touch the build, schema, packaging, or deployment.

## Stack constraints (non-negotiable)
- **Java 21**, **Spring Boot 4.0.6**, **Maven** (single module at repo root)
- **jOOQ** for DB access — **no JPA, no Hibernate**
- **Postgres 18** only (image: `postgres:18-alpine`)
- **`spring-boot-starter-webmvc`** (NOT `spring-boot-starter-web`)
- **`spring-boot-starter-flyway`** (NOT `flyway-core` directly)
- **Single Docker container**: Spring Boot serves both API and the bundled SPA from `src/main/resources/static/`
- **The jar is built by Maven, then copied into the runtime image.** The Dockerfile must NOT compile Java or build the frontend.

## Packaging
- Layer-based: `config/`, `audit/`, `data.repository/`, `data.service/`, `rest.controller/`, `rest.service/`, `rest.dto/`, `rest.exception/`. **No domain packages.**
- Base package: `io.github.pgatzka.projector`
- Generated jOOQ sources live under `target/generated-sources/jooq` (gitignored). Generated package: `io.github.pgatzka.projector.jooq`.
- **Use jOOQ-generated POJOs.** Do not hand-write POJOs that mirror tables.

## Layer rules
- **Controllers never call repositories.** Controller → `rest.service` → `data.service` (or `data.repository`).
- **Business logic lives in `rest.service`.** `data.service` is transactional CRUD per aggregate, no rules.

## Database conventions
- **All SQL is lowercase.** Identifiers are `snake_case`.
- **Table names are singular.** The users table is named `account` (not `user` — reserved keyword).
- **Mandatory column block on every table** (in this order):
  ```sql
  id          uuid                     not null default uuidv7() primary key,
  created_at  timestamp with time zone not null default current_timestamp,
  created_by  varchar(64)              not null,
  updated_at  timestamp with time zone not null default current_timestamp,
  updated_by  varchar(64)              not null,
  version     integer                  not null default 0,
  ```
- **Audit actor format**: `created_by` and `updated_by` are `varchar(64) not null`, application-enforced format `"user:<uuid>"`. **No FK** — actor prefixes (`user:`, `system:`, `job:`) are first-class.
- `set_updated_at()` trigger ignores `updated_at`, `updated_by`, and `version` when deciding whether to bump `updated_at`.
- **`AuditRecordListener`** sets `created_by`/`updated_by` automatically on insert/update — services must not set them manually.
- **Flyway** for migrations. One SQL statement per file. Filename shape per the database-design skill (`V<n>__create_table_<name>.sql`, etc.).
- **Whenever working on the database, the `database-design` skill MUST be used.**

## Frontend conventions
- Vite + React + TypeScript + Tailwind, `src/` rooted at `frontend/`
- Frontend `package.json` version is kept in lockstep with `pom.xml` via `npm pkg set version=${project.version}` during the Maven `validate` phase.
- **Whenever working on the frontend, the `frontend-design` skill MUST be used.**

## Build & local dev
- `compose.yaml` at repo root provides `postgres:18-alpine` for local dev. `spring-boot-docker-compose` auto-discovers it on `mvn spring-boot:run`.
- **No `SPRING_DATASOURCE_*` env vars in any config we own.** Spring reads from the running Postgres container (dev) or the operator's deployment env (prod).

## Testing
- **Unit tests**: `*Test.java`, surefire, **no Spring context**.
- **E2E tests**: `*IT.java`, failsafe, Cucumber 7 (Java + cucumber-spring + JUnit Platform Suite). Step glue: `io.github.pgatzka.projector.e2e`.
- A separate `postgres:18-alpine` test container is started by `docker-maven-plugin` on `pre-integration-test` (port 25432) and stopped on `post-integration-test`. Migrations run via `flyway-maven-plugin`.

## CI / image publishing
- GitHub Actions runs `mvn verify` and builds the Docker image on every push/PR.
- On `main` only, the image is pushed to GHCR with **two tags**: `${project.version}-${shortSha}` and `${project.version}`.
- **Never `:latest`.**

## Where things live
- Plans: `docs/superpowers/plans/`
- Reference codegen pom (kept for diffs): `docs/jooq-codegen-example/pom.xml`
```

- [ ] **Step 15.2: Verify length (<200 lines)**

Run: `wc -l CLAUDE.md`
Expected: line count under ~200. (If much higher, split into `.claude/docs/*.md` per the `claude-md` skill.)

- [ ] **Step 15.3: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: CLAUDE.md capturing durable project preferences"
```

---

## Task 16: README

**Files:**
- Create: `README.md`

- [ ] **Step 16.1: Create `README.md`**

````markdown
# Projector

Solo, self-hosted issue tracker. Single Docker container running Spring Boot 4 (REST API + bundled Vite/React SPA) backed by Postgres 18.

## Prerequisites
- JDK 21 (Temurin recommended)
- Maven 3.9+
- Docker (for local Postgres + image build)
- A running Postgres 18 instance (homeserver or container) for production

## Build, test, package
```bash
mvn verify
```
Runs: jOOQ codegen against a throwaway Postgres → unit tests → Cucumber E2E against a separate test Postgres → fat jar at `target/projector-1.0.0-SNAPSHOT.jar` (with the SPA bundled inside).

## Run locally
```bash
mvn spring-boot:run
```
`spring-boot-docker-compose` auto-starts `postgres:18-alpine` from `compose.yaml`. Open <http://localhost:8080>.

For frontend hot-reload, run Vite in parallel:
```bash
cd frontend && npm run dev
```
Then open <http://localhost:5173> (Vite proxies `/api` and `/actuator` to `:8080`).

## Build the Docker image
```bash
mvn package -DskipTests   # produce jar
docker build -t projector:dev .
```

## Run the image
```bash
docker run --rm -p 8080:8080 \
  -e SPRING_DATASOURCE_URL=jdbc:postgresql://<your-postgres-host>:5432/projector \
  -e SPRING_DATASOURCE_USERNAME=projector \
  -e SPRING_DATASOURCE_PASSWORD=<password> \
  projector:dev
```
Or include the app in your homeserver's compose stack alongside `postgres:18-alpine` and let `spring-boot-docker-compose` discover it.

## Endpoints (v0.1)
- `GET /api/health` — application health (always 200, body `{"status":"ok"}`)
- `GET /actuator/health` — Spring health
- `GET /actuator/prometheus` — Prometheus scrape
- `GET /v3/api-docs` — OpenAPI spec
- `GET /swagger-ui.html` — Swagger UI
- `GET /` — SPA (placeholder)

## Project conventions
See [`CLAUDE.md`](./CLAUDE.md). Particularly: layer packaging, mandatory audit columns, jOOQ-generated POJOs, no `:latest` image tag.
````

- [ ] **Step 16.2: Commit**

```bash
git add README.md
git commit -m "docs: README with build/run/test instructions"
```

---

## Task 17: End-to-end verification

This task confirms everything wired together actually works. No new files.

- [ ] **Step 17.1: Clean build**

Run: `mvn clean verify`
Expected: `BUILD SUCCESS`. All phases run: codegen DB → flyway → jOOQ codegen → frontend install + build → unit tests → test DB → flyway → cucumber → jar.

- [ ] **Step 17.2: Verify SPA bundled in jar**

Run: `unzip -l target/projector-1.0.0-SNAPSHOT.jar | grep "BOOT-INF/classes/static/index.html"`
Expected: one match.

- [ ] **Step 17.3: Build and run the Docker image against your homeserver Postgres**

Run:
```bash
docker build -t projector:v0.1-smoke .
```
Expected: `Successfully tagged projector:v0.1-smoke`.

Then (substituting real connection details):
```bash
docker run --rm -p 8080:8080 \
  -e SPRING_DATASOURCE_URL=jdbc:postgresql://<homeserver-pg-host>:5432/projector \
  -e SPRING_DATASOURCE_USERNAME=projector \
  -e SPRING_DATASOURCE_PASSWORD=<password> \
  projector:v0.1-smoke
```
Expected log:
- Flyway runs `V1__create_function_set_updated_at.sql` (or "schema is up to date" if already applied)
- `Started ProjectorApplication in N seconds`

- [ ] **Step 17.4: Smoke-test endpoints**

In another terminal:
```bash
curl -s http://localhost:8080/api/health
```
Expected: `{"status":"ok"}`.

```bash
curl -s http://localhost:8080/actuator/prometheus | head -3
```
Expected: prometheus comment + metric lines.

Open <http://localhost:8080> in a browser.
Expected: "Projector v0.1 skeleton" header, "status: ok" in green.

- [ ] **Step 17.5: Tear down**

`Ctrl+C` the docker run. Optionally:
```bash
docker rmi projector:v0.1-smoke projector:dev
```

- [ ] **Step 17.6: Push to remote and watch CI**

```bash
git push origin main
```
Expected: GitHub Actions runs the `CI` workflow → green build → image pushed to `ghcr.io/<owner>/projector:1.0.0-SNAPSHOT-<shortSha>` and `ghcr.io/<owner>/projector:1.0.0-SNAPSHOT`.

Verify on GHCR (<https://github.com/users/pgatzka/packages>) — both tags present, no `:latest`.

---

## Self-review

**Spec coverage:**

| Spec requirement | Task |
|---|---|
| Single Docker container, jar built outside | Task 12 |
| Spring Boot serves SPA + API | Task 7, 11 |
| jOOQ + codegen, generated POJOs only | Task 2 (pom inlines example), Task 5 (listener uses generic Record) |
| jOOQ generated sources gitignored | Task 1 (`target/`) |
| `spring-boot-starter-flyway` not `flyway-core` | Task 2 |
| `spring-boot-starter-webmvc` not `-web` | Task 2 |
| Postgres 18 (`-alpine` for dev) | Task 13, Task 2 (codegen + test) |
| No `SPRING_DATASOURCE_*` in our config | Task 3 |
| `compose.yaml` for spring-boot-docker-compose | Task 13 |
| Actuator + Prometheus on 8080, no auth | Task 3 (actuator config) + Task 4 (security permits) |
| `set_updated_at` ignores updated_at/updated_by/version | Task 3 |
| `AuditRecordListener` sets actor automatically | Task 5 |
| Layer packaging | Task 4–7 (every class placed by layer) + Task 15 (CLAUDE.md rule) |
| Controllers never call repositories | Task 15 (CLAUDE.md rule); v0.1 has no repos yet |
| `account` not `user` | Task 15 (CLAUDE.md rule); no tables in v0.1 |
| Mandatory column block + audit format | Task 15 (CLAUDE.md rule); no tables in v0.1 |
| Maven coordinates / version sync | Task 2 (pom), Task 10 (package.json), Task 2 (frontend-maven-plugin sync execution) |
| CI: test + jar + image; main pushes two tags, no `:latest` | Task 14 |
| Unit tests no Spring context | Task 6 |
| Cucumber E2E with separate test Postgres | Task 8, 9 |
| `database-design` skill required for DB work | Task 15 (CLAUDE.md rule) |
| `frontend-design` skill required for frontend work | Task 15 (CLAUDE.md rule) |
| Coordinates `io.github.pgatzka:projector:1.0.0-SNAPSHOT` | Task 2, 10 |
| Move `example.xml` → `docs/jooq-codegen-example/pom.xml` | Task 1 |

All spec items covered.

**Placeholder scan:** searched for "TODO", "TBD", "fill in", "implement later", "appropriate error handling", "similar to" — none present. Every code block is complete and runnable.

**Type consistency:**
- `HealthController.health()` returns `Map<String, String>` in Task 6 step 6.3; the test at step 6.1 asserts `Map<String, String>`. ✓
- `AuditRecordListener` constructor takes `ActorContext`; `JooqConfig` injects `AuditRecordListener` (which Spring then needs `ActorContext` for) — wiring chain is consistent. ✓
- `ActorContext.currentActor()` returns `String` everywhere it's used. ✓
- `Cucumber` glue path `io.github.pgatzka.projector.e2e` (Task 8) matches the package of `HealthSteps` (Task 9). ✓
- Frontend `package.json` version `1.0.0-SNAPSHOT` (Task 10) matches `pom.xml` version (Task 2). ✓
- Cucumber step text `"I request GET {string}"`, `"the response status is {int}"`, `"the response body field {string} equals {string}"` in Task 9 step 9.1 (feature) and step 9.3 (steps) match exactly. ✓

No issues found.
